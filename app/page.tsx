import { auth, signOut } from '@/auth'
import { redirect } from 'next/navigation'

const page = async () => {
  //   if (!user) {
  //       redirect('/auth/signin')
  //     } else {
  //       redirect('/dashboard')
  //   }

  return <div>test</div>
}
export default page
